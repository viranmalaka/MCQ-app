/**
 * Created by malaka on 8/12/17.
 */
var validator = require('validator');

var test = function (str, input) {
  return input.test(str);
};

// console.log(test('Al Ashraq Natio-nal School (M.m.v.), Nintavur',  ));